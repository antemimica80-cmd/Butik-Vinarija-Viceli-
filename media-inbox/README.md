# Not published

Files here are NOT part of the website (only `public/` is served).

- The five 1024×1536 bottle images (originals). They appear AI-generated; optimised copies are now used in the proposal at the owner's request and must be replaced with real packshots before launch.
  such as "VEHUNSKO VINO", "Snooth"; an unverifiable "top 1% of all wines" claim). The brief
  requires real photography only — replace them with real packshots before use.
- The original uploads of the photos now in `public/media/` (optimised copies are used on the site).
